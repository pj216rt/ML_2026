\documentclass[12pt]{article}

%packages
\usepackage{fancyhdr}
\usepackage[margin=1in]{geometry}
\usepackage[utf8]{inputenc}
\usepackage{listings}
\usepackage{enumitem}
\usepackage{pdfpages}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{float}
\usepackage{amsmath}


%title info
\newcommand{\doctitle}{STA 5635 HW 1}
\newcommand{\name}{Palmer Swanson}

\pagestyle{fancy}
\lhead{\textbf{\doctitle}}
\chead{\name}
\rhead{\today}

%document
\begin{document}




\section*{Problem 1}
\begin{enumerate}
    \item On the Gisette dataset, train decision trees of maximum depth 1,2,\(\cdots\) up to 12, for a total of 12 
    decision trees.  Use the trained trees to predict the class labels on the training and test sets
    and obtain the training and test misclassification errors.  Plot on the same graph the training and test 
    misclassification errors vs tree depth as two seperate curves.  Report in a table the minimum
    test error and the tree depth for which the minimum was attained.

    We do this in Python, and obtain the following plot of the training and test misclassification errors vs tree 
    depth:

    \begin{figure}[h]
        \centering
        \includegraphics[totalheight=8cm]{../figures/gisette_misclassification_error.png}
        \caption{Misclassification Error vs Tree Depth, Training and Validation: Gisette dataset}
        \label{fig:verticalcell}
    \end{figure}

    \item Repeat point 1.) on the satimage, madelon, hill-valley, and covtype datasets.
    
    We can do this also, and obtain the four images shown in figure \ref{fig:part_b}.

    \begin{figure}[h]
        \centering
        % First row of images
        \begin{minipage}{0.48\textwidth}
            \centering
            \includegraphics[width=\linewidth]{../figures/satimage_misclassification_error.png}
        \end{minipage}\hfill
        \begin{minipage}{0.48\textwidth}
            \centering
            \includegraphics[width=\linewidth]{../figures/madelon_misclassification_error.png}
        \end{minipage}
        
        \vspace{1cm} % Adds vertical space between rows
        
        % Second row of images
        \begin{minipage}{0.48\textwidth}
            \centering
            \includegraphics[width=\linewidth]{../figures/hill_valley_misclassification_error.png}
        \end{minipage}\hfill
        \begin{minipage}{0.48\textwidth}
            \centering
            \includegraphics[width=\linewidth]{../figures/Covtype_misclassification_error.png}
        \end{minipage}
        
        \caption{Training and Validation datasets misclassification error rates for four datasets}
        \label{fig:part_b}
    \end{figure}
    
    \item On the Gisette dataset, for each off \(k \in \{3,10,30,100,300\}\) train a random forest with \(k\) trees where
    the split attribute at each node is chosen from a random subset of \(71\approx\sqrt{5000}\) features.  Use the trained
    trrees to predict the class labels on the training and test sets and obtain the training and test misclassification
    errors.  Plot on the same graph the training and test errors vs. number of trees \(k\) as two seperate curves.  Report
    the training and test misclassification errors in a table.
    \item Repeat point 3.) on the satimage, madelon, and hill-valley datasets.
    \item Repeat point 3.) on the Gisette dataset where the split attribute at each node is chosen from a 
    random subset of \(12\approx \log_2(5000)\) features, and where the split attribute at each node is chosen
    from all 5000 features, plotting all four curves (2 train and 2 test misclassification errors) on the same graph.
\end{enumerate}


\end{document}