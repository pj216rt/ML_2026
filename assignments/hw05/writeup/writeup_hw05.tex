\documentclass[12pt]{article}

%packages
\usepackage{fancyhdr}
\usepackage[margin=1in]{geometry}
\usepackage[utf8]{inputenc}
\usepackage{listings}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{pdfpages}
\usepackage{graphicx}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{subcaption}
\usepackage{float}
\usepackage{amsmath}

%fancyheadr fix
\setlength{\headheight}{15pt}

%commands to lslistings
\lstset{
    basicstyle=\ttfamily\small,
    breaklines=true,     
    breakatwhitespace=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny,
    columns=fullflexible,
    keepspaces=true
}


%title info
\newcommand{\doctitle}{STA 5635 HW 5}
\newcommand{\name}{Palmer Swanson}
\newcommand{\docdate}{\today}


%document
\begin{document}

\begin{center}
    {\Large \doctitle}\\[6pt]
    {\name}\\[3pt]
    {\docdate}
\end{center}
\vspace{12pt}

\section*{Problem 1}

\begin{enumerate}[label=(1\alph*), ref=1\alph*]
    \item Loss function: 
    \(L(\boldsymbol{w}) = \frac{1}{N}\sum_{i=1}^{N}f(y_i\boldsymbol{x}_i^T\boldsymbol{w}) + 
    s\lvert\lvert\boldsymbol{w}\rvert\rvert_{2}^{2}\), where \(f(x) = \text{max}(0,1-x)\).  
    I do not believe that the derivative of \(f(x)\) exists, at least not cleanly.  The portion of the loss 
    consisting of the average of the function only contributes if the function doesn't return 0.  In other words:
    \[
    f(y_i\boldsymbol{x}_i^T\boldsymbol{w}) = \begin{cases} 
    0 & y_i\boldsymbol{x}_i^T\boldsymbol{w} \geq 1 \\
    1- y_i\boldsymbol{x}_i^T\boldsymbol{w} & y_i\boldsymbol{x}_i^T\boldsymbol{w} < 1
    \end{cases}
    \] 
    So for a point where \(y_i\boldsymbol{x}_i^T\boldsymbol{w} < 1\), the 
    gradient is \(\frac{\partial }{\partial \boldsymbol{w}}(1- y_i\boldsymbol{x}_i^T\boldsymbol{w}) = 
    -y_i\boldsymbol{x}_i^T\).  For a points where
    \(y_i\boldsymbol{x}_i^T\boldsymbol{w} \geq 1\), the gradient is 
    \(\frac{\partial }{\partial \boldsymbol{w}}(0) = 0\).  
    All together, the total gradient is:
    \[
    \nabla_{\boldsymbol{w}} = 
    \frac{-1}{N}\sum_{i:(y_i\boldsymbol{x}_i^T\boldsymbol{w}) < 1}(y_i\boldsymbol{x}_i^T) + 2s\boldsymbol{w}
    \]

    We plot the training loss vs. iteration number is Figure \ref{fig:SVM_training_loss}.  

    \begin{figure}[htbp] 
        \centering 
        \includegraphics[width=0.5\linewidth]{../figures/primal_SVM_training_loss.png} 
        \caption{Primal SVM Training Loss vs. Iterations} 
    \label{fig:SVM_training_loss} 
    \end{figure}
    \item Use SVM package to train linear SVM, C=1.
    We can report the training errors and number of support vectors 
    \item Use SVM package to train a SVM with polynomial kernel of degree 2.  
    \item Train an SVM classifer with C=1 and RBF kernels \(\gamma\).
    \item asdf
\end{enumerate}

\end{document}